[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# SPA catch-all
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
